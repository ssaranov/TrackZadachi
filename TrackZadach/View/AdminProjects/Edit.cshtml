@model TrackZadach.Models.Mission
@{
	ViewData["Title"] = "Редактирование проекта через MVC";
    var statues = new List<string>
    {
            "New",
            "Current",
            "Completed"
    };
}
<div class="container mt-5">
    <div class="row justity-content-center">
        <div class="col-md-8">

            <div class="card shadow">
                <div class="card-body">
                    <h1 class="mb-4">Редактирование через MVC</h1>
                    <form asp-controller="AdminProjects" asp-action="Edit" method="post">
                        @Html.AntiForgeryToken()
                        <input asp-formaction="Id" type="hidden" />

                        <div class="mb-3">
                            <label asp-formaction="Title" class="form-label">Название</label>
                            <input asp-for="NameTask" class="form-control" />
                            <span asp-validation-for="NameTask" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-formaction="Description" class="form-label">Описание</label>
                            <textarea asp-for="DescriptionTask" class="form-control" />
                            <span asp-validation-for="DescriptionTask" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-formaction="Status" class="form-label">Статус</label>
                            <select asp-for="Status" class="form-select">
                                <option value="">Выберите статус</option>
                                @foreach (var status in statues)
                                {
                                        <option value="@status">@status</option>
                                }
                            </select>
                            <span asp-validation-for="Status" class="text-danger"></span>
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-success">Сохранить</button>
                            <a asp-controller="AdminProjects" asp-action="Index" class="btn btn-secondary">Отмена</a>
                        </div>
                    </form>
                </div>
            </div>

        </div>
    </div>
</div>
